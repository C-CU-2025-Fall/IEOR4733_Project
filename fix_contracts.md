# fix_contracts.py — 数据修复逻辑说明

## 背景

CLC 数据集提供三种连续合约序列：

| 后缀 | 全称 | 含义 |
|------|------|------|
| `_RAD.CSV` | Ratio-Adjusted | 供应商做了展期比例调整的连续序列 |
| `_NON.CSV` | Non-Adjusted | 各月合约价格直接拼接，没有任何调整 |
| `_REV.CSV` | Reverse-Adjusted | 用反向调整因子的连续序列 |

部分合约（如 ZH、ZN、ZU）的 `_RAD` 在测试期（2011–2019）内价格为全 0 或存在数万倍的虚假跳空，无法直接使用。
`_NON` 虽然没有调整，但价格本身是真实交易价格，跳空仅来自自然展期差价（通常 <5%），适合作为修复底稿。

---

## 三种 CLC 文件的区别

```
价格序列示意图（以原油期货展期为例）：

_NON（未调整）:           _RAD（比例调整，但有错误）:
价格                      价格
  ┆   3月合约 4月合约       ┆   3月合约 4月合约
82┤  ┌────┐               82┤  ┌────┐
80┤  │    │               80┤  │    │
78┤  │    └─┬─────        78┤  │    │
  ┤  │      │             ┆┤  │    │
  ┤           4月合约        ┆┤  │    xxx ← 调整因子错误，价格跳到几万
     展期时    跳了 2块             展期时  数万倍的跳空
     这是真实的差价             

_NON 的跳空大多是正常的展期差价（1-5%）
_RAD 的问题是调整因子累积错误，导致价格尺度失控
```

---

## fix_contracts.py 的修复方法（方案 B — 比例拼接）

核心思路：以 `_NON` 为底稿，扫描序列中每一个"过大跳空"，在该跳空位置重新校准调整因子，把后续所有价格拉回正常尺度。

### 逐步流程

**第一步：读取 `_NON.CSV`**

```
原始序列（_NON）：
日期          收盘价
2011-01-03    255.28
2011-01-04    250.65   ← 跌 1.8%，正常
2011-01-05    254.22   ← 涨 1.4%，正常
...
2012-03-15    480.00   ← 正常
2012-03-16    384.00   ← 跌 19.8%，超过 10% 阈值！(展期造成的虚假断点)
2012-03-17    388.00
```

---

**第二步：遇到超阈值跳空，计算新的修正因子**

$$\text{factor}_{\text{new}} = \frac{P_{t-1}^{\text{adjusted}}}{P_t^{\text{raw}}}$$

具体过程：

```
前一日已调整收盘：480.00（保持不变）
当日原始收盘：   384.00

factor_new = 480.00 / 384.00 = 1.25

意思是：从这一天开始，所有原始价格都 × 1.25
这样今天的调整收盘就变成 384.00 × 1.25 = 480.00
与昨天持平，消除了这个虚假跳跃
```

---

**第三步：将新因子应用到当天及其后所有行**

```
调整过程（factor 累积传播）：

日期          原始价(NON)  当前factor   调整后价格
...
2012-03-15    480.00       1.00         480.00
2012-03-16    384.00       1.25 ← 更新  480.00    ← 跳空消除
2012-03-17    388.00       1.25         485.00
2012-03-18    391.00       1.25         488.75
...
（如果后面又出现新的超阈值跳空，factor 再次更新）
```

---

**整个逻辑的伪代码：**

```python
factor = 1.0
prev_close = None

for 每一行 row in NON序列:
    raw_close = row.close

    if prev_close is not None and raw_close > 0:
        projected = raw_close * factor          # 用当前factor投影当日收盘
        jump = |projected / prev_close - 1|     # 计算跳幅

        if jump > threshold:                    # 超过阈值
            factor = prev_close / raw_close     # 重新校准factor
            # 效果：raw_close * factor == prev_close，跳空归零

    adj_close = raw_close * factor              # 用（可能更新的）factor调整价格
    prev_close = adj_close                      # 保存为下一行的"前收盘"
    输出 [date, adj_open, adj_high, adj_low, adj_close, vol, oi]
```

---

## 关键性质

| 性质 | 说明 |
|------|------|
| **只消除机械断点** | 正常交易日（跳幅 < threshold）的价格完全不变 |
| **因子向前传播** | 一次修正会影响后续所有价格，保持序列连续 |
| **不修改量价** | Volume 和 OI 列保持原始 NON 数据 |
| **OHLC 一致性** | Open/High/Low/Close 乘以同一个 factor，OHLC 相对关系不变 |
| **可复现** | 只要调整阈值参数 `threshold` 固定，结果唯一确定 |

---

## 阈值选择的影响

| threshold | 效果 | 适用场景 |
|-----------|------|---------|
| **0.05（5%）** | 更激进修复，展期差价 >5% 都被消除 | 数据问题严重的合约（如 ZN） |
| **0.10（10%）** | 默认，消除明显的机械断点，保留市场真实极端日 | 通用 |
| **0.15（15%）** | 保守修复，只处理极端断点 | 数据质量较好的合约 |

实际市场中期货价格单日涨跌超过 10% 的情况非常罕见（仅限黑天鹅事件），因此 10% 是一个合理的默认值。

---

## 运行方式

```bash
# 默认阈值 10%，修复全部 50 个合约
python fix_contracts.py

# 调整阈值
python fix_contracts.py --threshold 0.05
python fix_contracts.py --threshold 0.15

# 指定路径
python fix_contracts.py --source-dir /path/to/CLCDATA --target-dir data/CLC
```

运行后生成 `data/CLC/{ticker}_FIXED.CSV`，Loader 会自动对所有合约优先读取 FIXED 版本。

---

## 和原始数据的关系

```
CLCDATA/ZH_NON.CSV   ← 输入（底稿，不修改）
CLCDATA/ZH_RAD.CSV   ← 原始坏数据（不修改，不读取）
CLCDATA/ZH_REV.CSV   ← 备用版本（不修改，不读取）

data/CLC/ZH_FIXED.CSV  ← 输出（修复结果，loader 读这个）
```

---

## 局限性

1. **不是真正的 ratio-adjusted**：修复结果是"拼接版连续合约"，不等于供应商的标准比例调整方法。
2. **跨越真实极端行情的风险**：如果某个真实黑天鹅事件恰好也产生 >10% 的单日跳动，会被错误地修正掉。
3. **价格绝对值不可信**：调整后序列的价格绝对值没有经济含义，只有相对收益率是可信的。

---

*生成日期：2026-04-12 | 脚本：`fix_contracts.py` | 数据来源：Pinnacle Data Corp CLC Database*
