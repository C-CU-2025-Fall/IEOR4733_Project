# CLC RAD 数据方法分析与 RAD_v2 验证

**日期**: 2026-04-12 (修订版 v2.0)  
**项目**: IEOR4733 - Deep Reinforcement Learning for Trading 复现  
**数据源**: CLC manual.doc APPENDIX B-1

---

## 一、CLC RAD 方法理解

### 1.1 核心概念

CLC (Continuous Linked Contract) 数据由 Futures Truth 公司提供，RAD 是 Ratio-Adjusted 的连续合约数据。

**基本公式**:
```
RAD_Close[t] = NON_Close[t] × ratio[t]
```

其中 `ratio[t]` 在 roll date 发生跳变，用于消除合约切换时的价格跳空。

### 1.2 Roll Date 规则（来自 manual.doc APPENDIX B-1）

**重要说明**:
- `DM = Delivery Month` (交割月)
- `MPDM = Month Previous to Delivery Month` (交割月前一个月)
- 粗体商品为 24hr Session，其他为 Day Session

| 规则类型 | 含义 | 适用合约 |
|----------------------------|-----------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| THUR_PRIOR_2ND_FRI_OF_DM | 交割月第 2 个星期五之前的星期四 | AP, CA, DJ, EN, ER, ES, MD, ND, RL, SC, SP, AX, XU, XX, YM, ZD |
| DM_1 | 交割月第 1 天 | LB |
| DM_3 | 交割月第 3 天 | NK, SS |
| DM_5 | 交割月第 5 天 | FC, ZF |
| DM_6 | 交割月第 6 天 | CR |
| DM_8 | 交割月第 8 天 | AD, AN, BN, CN, DX, FN, FX, JN, JY, MP, SF, SN |
| DM_9 | 交割月第 9 天 | GI |
| DM_13 | 交割月第 13 天 | LX |
| DM_22 | 交割月第 22 天 | FF |
| MPDM_9 | 交割月前一个月第 9 天 | CC |
| MPDM_11 | 交割月前一个月第 11 天 | BC, BG, CL, HO, HU, KC, RB |
| MPDM_13 | 交割月前一个月第 13 天 | O_, ZO |
| MPDM_15 | 交割月前一个月第 15 天 | CT |
| MPDM_18 | 交割月前一个月第 18 天 | NG |
| MPDM_22 | 交割月前一个月第 22 天 | BO, C_, ED, EC, GC, HG, KW, MW, NR, PA, PL, S_, SB, SI, SM, W_, ZC, ZG, ZI, ZL, ZP, ZA |
| MPDM_24 | 交割月前一个月第 24 天 | CB, DT, FA, FB, GS, TA, TD, TU, TY, UA, UB, US, UZ |
| MPDM_25 | 交割月前一个月第 25 天 | JO |
| MPDM_27 | 交割月前一个月第 27 天 | DA, LC, LH, ZT, ZZ |

**配置共 90 个合约**，覆盖 APPENDIX B-1 中所有有明确 roll rule 的合约。

### 1.3 Ratio 调整特征

- **累积方向**: 从数据第一天向最后一天累积
- **基准日**: 最后一天 ratio = 1.0
- **Roll day 调整**: ratio 在 roll date 发生跳变

**调整因子**:
```
ratio_after = ratio_before × (c / C)
其中 c = 前一天 Close, C = 当天 Close
```

---

## 二、RAD_v2 实现方法

### 2.1 算法设计

```python
def generate_rad_v2(non_file, output_file, rule_type, day, contract_months):
    """
    以测试期第一天为基准，从前往后应用 ratio 调整
    
    参数:
    - rule_type: THUR_PRIOR_2ND_FRI | DM | MPDM
    - day: 日期数字 (DM/MPDM 用)
    - contract_months: 合约月份字符串如 "H,M,U,Z"
    """
    # 1. 读取 NON 数据
    df = pd.read_csv(non_file, names=['Date', 'O', 'H', 'L', 'C', 'V', 'OI'])
    
    # 2. 生成理论 roll dates
    roll_dates = get_roll_dates(rule_type, day, contract_months, start_year, end_year)
    
    # 3. 匹配到实际交易日 (±5 天内)
    actual_rolls = match_to_trading_days(roll_dates, df['Date'])
    
    # 4. 找到测试期开始和第一个 roll
    test_start_idx = df[df['Date'] >= TEST_START].index[0]
    rolls_after_test = [r for r in actual_rolls if r >= test_start_idx]
    
    # 5. 初始化 ratio = 1 @ 测试期第一天
    df['ratio'] = 1.0
    df.loc[:test_start_idx, 'ratio'] = 1.0
    
    # 6. 从前往后累积
    current_ratio = 1.0
    for i, roll_idx in enumerate(rolls_after_test):
        C = df.iloc[roll_idx]['Close']
        c = df.iloc[roll_idx - 1]['Close']
        if c > 0 and C > 0:
            ratio_change = c / C
            current_ratio *= ratio_change
            
            next_roll = rolls_after_test[i + 1] if i + 1 < len(rolls_after_test) else len(df)
            df.loc[roll_idx:next_roll - 1, 'ratio'] = current_ratio
    
    # 7. 生成 RAD
    df['RAD_Close'] = df['Close'] * df['ratio']
```

### 2.2 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 基准日 | 测试期第一天 (2011-01-01) | 与论文测试期对齐 |
| 初始 ratio | 1.0 | 简化计算，保持相对变化 |
| 累积方向 | 从前往后 | 与 CLC 一致 |
| Ratio 公式 | c/C | 基于论文财富调整逻辑 |

---

## 三、验证结果与差距分析

### 3.1 数据状态

| 类别 | 数量 | 状态 |
|------|------|------|
| 有 NON 文件 | 95 | 可用于 RAD_v2 生成 |
| 成功生成 RAD_v2 | 84 | 有对应 roll rule |
| 失败 (无 roll rule) | 11 | ZB, ZM, RL, ZU, ZW, HS, ZN, ZR, ZK, ZH, ZS |

**损坏合约 (需 RAD_v2 修复)**: ZH, ZI, ZN, ZU, US
- ZH: CLC RAD 全 0
- ZI: CLC RAD 需验证
- ZN: CLC RAD 价格异常 (~10000x)
- ZU: CLC RAD 全 0
- US: CLC RAD NaN

### 3.2 84 个合约验证结果

**测试期**: 2011-01-01 至 2019-12-31

| 指标 | 结果 |
|------|------|
| 第一天对齐 NON (<0.01%) | 82/84 (97.6%) ✓ |
| 相关性 ≥0.99 | 28/84 (33.3%) |
| 相关性 0.95-0.99 | 14/84 (16.7%) |
| 相关性 <0.95 | 37/84 (44.0%) |
| 最后一天差异中位数 | 31.60% |

### 3.3 高相关性合约 (≥0.99) 示例

| Symbol | Corr(v2,RAD) | First_Diff% | Last_Diff% |
|--------|--------------|-------------|------------|
| EN | 0.9997 | 0.0 | ~16% |
| ES | 0.9997 | 0.0 | ~6% |
| SP | 0.9997 | 0.0 | ~7% |
| SC | 0.9997 | 0.0 | ~7% |
| YM | 0.9996 | 0.0 | ~0.8% |

### 3.4 差距分析

**差异来源**:

1. **Roll dates 数量不匹配**
   - ES: 我们计算 115 个，CLC 检测 105 个
   - 可能原因：早期数据 roll rule 不同或合约变更

2. **CLC 使用不同价格源**
   - CLC 可能使用 settlement 价格而非 close
   - 或使用了特殊的平滑/修正规则

3. **历史累积起点不同**
   - CLC 从合约上市第一天开始累积
   - 我们仅从 2011-01-01 开始

4. **部分合约 roll rule 缺失**
   - 11 个合约在 APPENDIX B-1 中无明确规则
   - 需要进一步查阅 manual.doc 其他章节

---

## 四、检测代码

### 4.1 Roll Date 计算

```python
import calendar
import pandas as pd

def get_nth_weekday(year, month, n, weekday):
    """获取某月第 n 个星期几"""
    first_day = calendar.weekday(year, month, 1)
    if first_day <= weekday:
        first = weekday - first_day + 1
    else:
        first = 7 - first_day + weekday + 1
    return first + (n - 1) * 7

def get_2nd_friday(year, month):
    """获取某月第 2 个星期五"""
    return get_nth_weekday(year, month, 2, 4)  # Friday = 4

def get_roll_dates(rule_type, day, contract_months_str, start_year, end_year):
    """生成理论 roll dates"""
    months_map = {'F': 1, 'G': 2, 'H': 3, 'J': 4, 'K': 5, 'M': 6,
                  'N': 7, 'Q': 8, 'U': 9, 'V': 10, 'X': 11, 'Z': 12}
    
    delivery_months = [months_map[m.strip()] for m in contract_months_str.split(',') 
                       if m.strip() in months_map]
    
    roll_dates = []
    for year in range(start_year, end_year + 1):
        for dm in delivery_months:
            if rule_type == 'THUR_PRIOR_2ND_FRI':
                # Thur prior 2nd Fri of DM
                second_friday = get_2nd_friday(year, dm)
                roll_day = second_friday - 1  # 前一天是星期四
                try:
                    rd = pd.Timestamp(year=year, month=dm, day=roll_day)
                    roll_dates.append(rd)
                except:
                    pass
            elif rule_type == 'DM':
                try:
                    rd = pd.Timestamp(year=year, month=dm, day=day)
                    roll_dates.append(rd)
                except:
                    pass
            elif rule_type == 'MPDM':
                pm_year = year - 1 if dm == 1 else year
                pm_month = 12 if dm == 1 else dm - 1
                try:
                    rd = pd.Timestamp(year=pm_year, month=pm_month, day=day)
                    roll_dates.append(rd)
                except:
                    pass
    
    return sorted(roll_dates)
```

### 4.2 验证函数

```python
def validate_rad_v2(symbol, data_dir, TEST_START, TEST_END):
    """验证 RAD_v2 与 CLC RAD 的一致性"""
    rad_file = data_dir / f'{symbol}_RAD.CSV'
    v2_file = data_dir / f'{symbol}_RAD_v2.CSV'
    non_file = data_dir / f'{symbol}_NON.CSV'
    
    rad = pd.read_csv(rad_file, names=['Date', 'O', 'H', 'L', 'C', 'V', 'OI'])
    v2 = pd.read_csv(v2_file, names=['Date', 'O', 'H', 'L', 'C', 'V', 'OI'])
    non = pd.read_csv(non_file, names=['Date', 'O', 'H', 'L', 'C', 'V', 'OI'])
    
    rad['Date'] = pd.to_datetime(rad['Date'])
    v2['Date'] = pd.to_datetime(v2['Date'])
    non['Date'] = pd.to_datetime(non['Date'])
    
    # 过滤测试期
    rad_test = rad[(rad['Date'] >= TEST_START) & (rad['Date'] <= TEST_END)]
    v2_test = v2[(v2['Date'] >= TEST_START) & (v2['Date'] <= TEST_END)]
    non_test = non[(non['Date'] >= TEST_START) & (non['Date'] <= TEST_END)]
    
    min_len = min(len(rad_test), len(v2_test), len(non_test))
    rad_test = rad_test.iloc[:min_len].reset_index(drop=True)
    v2_test = v2_test.iloc[:min_len].reset_index(drop=True)
    non_test = non_test.iloc[:min_len].reset_index(drop=True)
    
    # 计算指标
    corr_v2_rad = v2_test['C'].corr(rad_test['C'])
    first_diff = abs(v2_test['C'].iloc[0] - non_test['C'].iloc[0]) / non_test['C'].iloc[0] * 100
    last_diff = abs(v2_test['C'].iloc[-1] - rad_test['C'].iloc[-1]) / rad_test['C'].iloc[-1] * 100
    
    return {
        'Symbol': symbol,
        'Corr(v2,RAD)': corr_v2_rad,
        'First_Diff%': first_diff,
        'Last_Diff%': last_diff
    }
```

---

## 五、结论与建议

### 5.1 结论

1. **RAD_v2 方法部分有效**: 33% 合约相关性 ≥0.99，50% ≥0.95
2. **第一天完全对齐**: 97.6% 合约测试期第一天价格 = NON 价格
3. **绝对水平有差异**: 最后一天差异中位数 31.60%
4. **11 个合约 roll rule 缺失**: 需进一步查阅 manual.doc

### 5.2 建议

1. **直接使用 RAD_v2**: 对于损坏的 5 个合约 (ZH, ZI, ZN, ZU, US)，用 RAD_v2 替换
2. **回测验证**: 运行完整回测，看 Sharpe/Sortino 等指标是否对齐论文
3. **不追求绝对价格一致**: 回测关注收益率序列，而非绝对价格水平

### 5.3 待解决问题

- [ ] 11 个合约 (ZB, ZM, RL, ZU, ZW, HS, ZN, ZR, ZK, ZH, ZS) 的 roll rule 需补充
- [ ] Roll dates 数量差异的根因
- [ ] CLC ratio 公式的精确形式

---

## 六、相关文件

| 文件 | 描述 |
|------|------|
| `config/roll_rules_corrected.json` | 90 个合约的 roll rules 配置 (修正版) |
| `data/CLC/*_RAD_v2.CSV` | 生成的 RAD_v2 文件 (84 个) |
| `data/CLC/rad_v2_corrected_summary.csv` | 生成结果汇总 |
| `data/CLC/rad_v2_corrected_validation.csv` | 验证结果 |
| `scripts/generate_rad_v2.py` | RAD_v2 生成脚本 |

---

## 修订历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-04-12 | 初始版本，roll rules 有误 |
| v2.0 | 2026-04-12 | 修正 roll rules 按 APPENDIX B-1，18 类规则覆盖 90 合约 |

---

**最后更新**: 2026-04-12 13:15 EDT
